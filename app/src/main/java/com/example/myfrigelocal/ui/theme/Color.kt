package com.example.myfrigelocal.ui.theme

import androidx.compose.ui.graphics.Color

/** Mint green for selected bottom nav item (design reference). */
val BottomNavSelected = Color(0xFF32E0A1)

/** Light bluish-gray for unselected bottom nav items. */
val BottomNavUnselected = Color(0xFFAAB4C1)

val BottomNavBarBackground = Color(0xFFFFFFFF)

// ui/theme/Color.kt
val FreshGreen = Color(0xFF4ADE80)
val FreshGreenDark = Color(0xFF22C55E)
val LightGray = Color(0xFFF5F5F5)